<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>School WhatsApp Outreach Tool</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f9f9f9;
      text-align: center;
      padding: 40px;
    }
    h1 { color: #2c3e50; }
    form {
      background: white;
      padding: 20px;
      border-radius: 10px;
      max-width: 500px;
      margin: 30px auto;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    input, select, textarea, button {
      width: 100%;
      margin: 10px 0;
      padding: 10px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 8px;
    }
    button {
      background-color: #25D366;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background-color: #1ebe5d;
    }
    .note { color: #888; font-size: 14px; margin-top: 10px; }
    .locked-message {
      background-color: #fff3cd;
      color: #856404;
      border: 1px solid #ffeeba;
      padding: 20px;
      max-width: 500px;
      margin: 30px auto;
      border-radius: 8px;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <h1>📢 School WhatsApp Outreach</h1>
  <p id="trial-status">Checking trial status...</p>

  <!-- TRIAL EXPIRED MESSAGE -->
  <div id="lockedMessage" class="locked-message" style="display: none;">
    ⛔ Your 3-day trial has expired.<br>
    Please <a href="https://wa.me/233XXXXXXXXX" target="_blank">contact support</a> to upgrade and unlock full access.
  </div>

  <!-- MESSAGE FORM -->
  <form id="whatsappForm" onsubmit="sendMessage(); return false;">
    <input type="text" id="school" placeholder="School Name" required>
    <select id="type" required>
      <option value="">Select Message Type</option>
      <option>Ad</option>
      <option>Announcement</option>
      <option>Invitation</option>
    </select>
    <textarea id="message" rows="5" placeholder="Your message..." required></textarea>
    <input type="text" id="number" placeholder="WhatsApp Number (without +)" required>
    <button type="submit" id="sendBtn">Send via WhatsApp</button>
    <div class="note">* Trial lasts for 3 days from first visit. Upgrade to continue.</div>
  </form>

  <script>
    function checkTrial() {
      const start = localStorage.getItem('trialStart');
      const now = new Date();

      if (!start) {
        localStorage.setItem('trialStart', now.toISOString());
        document.getElementById('trial-status').textContent = "✅ Trial started: Day 1 of 3";
        return true;
      }

      const startDate = new Date(start);
      const diff = Math.floor((now - startDate) / (1000 * 60 * 60 * 24));

      if (diff < 3) {
        document.getElementById('trial-status').textContent = `✅ Trial active: Day ${diff + 1} of 3`;
        return true;
      } else {
        // Trial expired
        document.getElementById('trial-status').textContent = "⛔ Trial expired";
        document.getElementById('whatsappForm').style.display = "none";
        document.getElementById('lockedMessage').style.display = "block";
        return false;
      }
    }

    function sendMessage() {
      if (!checkTrial()) {
        alert("Trial expired. Please contact us to upgrade.");
        return;
      }

      const school = document.getElementById('school').value;
      const type = document.getElementById('type').value;
      const message = document.getElementById('message').value;
      const number = document.getElementById('number').value;

      const fullMessage = `📣 [${school}] - ${type}\n\n${message}`;
      const encoded = encodeURIComponent(fullMessage);
      const whatsappURL = `https://wa.me/${number}?text=${encoded}`;

      window.open(whatsappURL, '_blank');
    }

    // Run on page load
    checkTrial();
  </script>
</body>
</html>
<div id="clock" style="font-size: 20px; font-weight: bold; margin-top: 20px;"></div>

<script>
  function updateClock() {
    const now = new Date();
    const time = now.toLocaleTimeString();
    document.getElementById('clock').innerText = '🕒 ' + time;
  }

  setInterval(updateClock, 1000);
  updateClock(); // Call it once to show immediately
</script>
